
def jobConfigurator = new JobConfigurator(this)
masterMap = jobConfigurator.getMasterMap(Sites.inclusiveHosts, Sites.inclusiveProducts)


masterMap.each {
  def jobName = it.key
  println "generating " + jobName
  jobConfigurator.createJob(jobName, masterMap)
}
